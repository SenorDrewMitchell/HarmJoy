<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>OpenRallyMap</title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
    <!-- Leaflet -->    
     <link rel="stylesheet" href="http://cdn.leafletjs.com/leaflet-0.7.3/leaflet.css" />
     <script src="http://cdn.leafletjs.com/leaflet-0.7.3/leaflet.js"></script>
    <!-- MapQuest --> 
    <script src="http://www.mapquestapi.com/sdk/leaflet/v1.s/mq-map.js?key=Fmjtd%7Cluurn90rn1%2C7g%3Do5-9wtn0z"></script>
  </head>

  <body>
    <nav class="top-bar" data-topbar role="navigation">
      <ul class="title-area">
        <li class="name">
          <h1><%= link_to "OpenRallyMap", root_path %></h1>
        </li>
      </ul>

      <section class="top-bar-section">
        <!-- Right Nav Section -->
		<ul class="right">
			<li class="has-dropdown">
				<a href="#">
					<i class="fi-list"></i>
				</a>
				<ul class="dropdown">
					<li><a href="#">First link in dropdown</a></li>
					<% if contributor_signed_in?%>
						<li>
							<%= link_to raw('<i class="fi-home"></i> dashboard'), dashboard_path %>
						</li>
						<li>
							<%= link_to raw('<i class="fi-torso"></i> sign out'), destroy_contributor_session_path, :method => :delete %>
						</li>
					<% else%>
						<li>
							<%= link_to raw('<i class="fi-torso"></i> sign in'), new_contributor_session_path %>
						</li>
					<% end %>
				</ul>
			</li>
		</ul>

        <!-- Left Nav Section -->
        <ul class="left">
		<li class="has-dropdown">
			<a href="#">
				<%
					current_race_day = RaceDay.where("race_date = '#{Date.today}'").first
					current_race_day = current_race_day.nil? ? Date.today : current_race_day
				%>
				
				<i class="fi-calendar"></i> <%= current_race_day %>
			</a>
			<ul class="dropdown">
				<% RaceDay.all.each do |race_date| %>
					<li>
						<a href="#"><%= "#{race_date.name} : #{race_date.race_date}"%></a>
					</li>
				<% end %>
			</ul>
		</li>
        </ul>
      </section>
    </nav>
    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>